profiles {	
    standard {	
        process {	
            withName: 'Demux' {	
                cpus = 8
                memory = 64.GB	
            }	
        }
    }
    
// Docker always true. Containers defined in each process
docker.enabled = true
}
