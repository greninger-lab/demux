profiles {	
    standard {	
        process {	
            withName: 'Trimming' {	
                cpus = 8
                memory = 64.GB	
            }	
        }
    }
    
// Docker always true. Containers defined in each process
docker.enabled = true
}
